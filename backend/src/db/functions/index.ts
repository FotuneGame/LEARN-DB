import findProblemsByEmployee from "./find_problems_by_employee";
import findByTheme from "./find_by_theme";
import { ObjectsType } from "../types";

export const dbFunctions:ObjectsType = {
    findProblemsByEmployee,
    findByTheme
} 