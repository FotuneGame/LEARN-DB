import clientCallbacks from "./clientCallbacks";
import clientEmployee from "./clientEmployee";
import { ObjectsType } from "../types";

export const dbRepresentation:ObjectsType = {
    clientCallbacks,
    clientEmployee
}